function Tooltip() {
  const handleTooltip = () => {};

  return (
    <div className="container">
      <button className="btn" onClick={handleTooltip}>
        Hover Over Me
      </button>
    </div>
  );
}

export default Tooltip;
